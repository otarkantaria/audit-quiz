services:
  quiz-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_db
      - quiz_data:/app/data
    restart: unless-stopped

volumes:
  chroma_data:
  quiz_data:
