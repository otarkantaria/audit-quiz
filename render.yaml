services:
  - type: web
    name: audit-quiz
    runtime: docker
    envVars:
      - key: DATA_DIR
        value: /app/data
    disk:
      name: quiz-data
      mountPath: /app/data
      sizeGB: 1
